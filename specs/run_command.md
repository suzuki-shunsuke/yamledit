# run command

```sh
yamledit run [<yaml file>...]
```

Edits YAML files.
Reads migration files from `.yamledit/*.yaml` and applies migrations according to the configuration.
About migration files, please see [migration_file.md](migration_file.md)
