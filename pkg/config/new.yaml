# yaml-language-server: $schema=https://raw.githubusercontent.com/suzuki-shunsuke/yamledit/main/json-schema/migration.json
rules:
  - path: "$"
    actions:
      - type: remove_keys
        keys:
          - age
